import { createTheme } from '@mui/material';

const dark = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#bd0411',
    },
    secondary: {
      main: '#303030',
    },
  },
});

export default dark;
