import { createTheme } from "@mui/material";

const dark = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#650006"
        },
        secondary: {
            main: "#c0c0c0"
        }
    }
});

export default dark;