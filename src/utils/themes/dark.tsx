import { createTheme } from "@mui/material";

const dark = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#650006",
        },
        secondary: {
            main: "#404042",
        }
    }
});

export default dark;